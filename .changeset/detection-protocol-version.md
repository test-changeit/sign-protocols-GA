---
'@rosen-bridge/detection': major
---

`GuardDetection` now declares a `protocolVersion` ('1.0'), signed and enforced by `Communicator`; guards on a different detection protocol version are rejected instead of processed
